# D02 lexical-drift corrective receipt

- Task: `unblock/vpn/67238de3eb987139/resolve`
- Corrective implementation commit: `271033767e1e9ef1512d2bf68340710fceb2caf5`
- Parent diagnosis: `/home/mesh-home/lte-workstation/docs/task-receipts/unblock-vpn-67238de3eb987139-20260911.md`

## Change

`scripts/drift_lexical.py` now reads the extractor's immutable `*-files.tsv`
inventories and lexical aggregates. Duplication is reported only when an added
path reuses an existing blob while the original remains; rename is reported
only for deleted/added paths sharing a blob; shuffle is reported only for
changed same-path blobs with an unchanged lexical profile. Each control now
includes its evidence (`duplicate_paths`, rename `pairs`, or `changed_paths`),
and absent controls produce negative verdicts.

`scripts/test_drift_lexical.py` now resets the temporary Git snapshot between
commits, adds an absent-rename negative assertion, and covers the evidence-based
shuffle path.

## Verification

```text
python3 scripts/test_drift_lexical.py
Ran 7 tests ... OK
artifact: /tmp/d02-lexical-tests.4MhLHq.txt
sha256: 12c584c8ca091f8fc68c188b42fde7ea549142dea7a6efc931c823ff655ad4d3
```

The broader script discovery ran 121 tests but remains unavailable as a clean
suite on this node because unrelated tests require missing `torch` and `numpy`.

## Handoff

VPN can resume D02-V from commit `271033767e1e9ef1512d2bf68340710fceb2caf5`,
rerun in a fresh isolated directory, and mutate a load-bearing behavior for
independent verification.
