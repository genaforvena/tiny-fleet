# S01 implementation receipt — preregister fleet study

- Actor: `haunt`
- Repository: `/home/mesh-home/tiny-fleet`
- UTC: `2026-09-11`
- Task: `tinyfleet-publication-science-20260908/preregister-fleet-study`
- Scope: `docs/study-registration.md`, `runs/fleet-study-v1/registration.json`, `docs/related-work.md`

## Result

Registered the held-out study question, five comparison arms, three separate outcomes, source-group
sampling and minimum independent units, 10,000-replicate paired bootstrap, seeds 17/29/43, explicit
quality/safety/coverage margins, and resource caps. The registration freezes the configuration before
held-out predictions and requires a new study ID for design changes. No held-out predictions were
generated or inspected as part of this preregistration.

## Verification

The prescribed registration syntax check was run from the repository root:

```text
rtk proxy .venv/bin/python -m json.tool runs/fleet-study-v1/registration.json
```

Exit code: `0`. `rtk git diff --check` also exited `0`. The four scoped artifact SHA-256 values at
receipt creation were recorded before commit; the exact source revision is the commit containing
these files.

## Limitations

The source corpus and runners are not generated by this task; the independent corpus freeze, matched
runner, scoring, calibration, and replication tasks remain successors. Related-work citations are
context only and are not outcome or decision inputs.

## Next action

Submit to `vpn` for independent S01 verification. The verifier must inspect this commit in isolation,
re-run the JSON check, recompute the declared counts and predicates, and write `S01-verification.md`.
