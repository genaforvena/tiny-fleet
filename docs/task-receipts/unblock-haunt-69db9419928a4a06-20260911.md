# Resolver receipt — `unblock/haunt/69db9419928a4a06/resolve`

- Date: 2026-09-11 UTC
- Owner: `haunt`
- Parent: `haunt-install-unblock-20260907/install-and-retry-tinyfleet`
- Repository revision inspected: `daf45d3bd456f3a30fc523031156336d5bab18fe`
- Result: `BLOCKED/operator-input` (`BLOCKED_DICTIONARY_INPUT`)

## Diagnosis

The install receipt records a successful protobuf/runtime repair and six-case
BbyWVY smoke generation. Its remaining dictionary arm requires a frozen
scientific input. The current tracked-file search found no dictionary,
lexicon, vocabulary, or word-list corpus, and a search outside receipts and
README prose found no operator-approved dictionary declaration. The A08
synthetic correction fixture is not sufficient: it does not specify the
install arm's normalization, immutable version, or corpus.

Evidence:

- `docs/task-receipts/haunt-install-unblock-20260908.md`
- `docs/task-receipts/haunt-install-unblock-20260909.md`
- `docs/task-receipts/haunt-unblock-a4d63bcf78d3af67-20260911.md`
- Tracked-file check: `git ls-files | rg -i '(^|/)(dictionary|lexicon|vocab|word[-_]?list)([^/]*|/)'` (no matches)

## Exact operator input required

Supply a tracked, immutable study-input record containing all five fields:

```yaml
source: <approved dictionary/lexicon dataset and canonical retrieval URL or local source>
language: <language or locale covered>
normalization: <executable Unicode and token normalization policy>
version: <immutable release, commit, or source checksum plus retrieval date>
corpus: <exact corpus path/URI and SHA-256>
```

The corpus hash must match the bytes used by the dictionary arm, and the
normalization must match an executable implementation. A package name,
unversioned URL, prose preference, or the unrelated A08 fixture does not
clear the blocker. Do not choose or download a dictionary on the operator's
behalf; that would supply the experiment's missing scientific input.

## Exact retry condition

After the input record and corpus are supplied, verify the corpus hash, record
the model/runtime versions and command output, then rerun the parent task's
dependency preflight and dictionary arm. The existing runtime smoke command
is `timeout 240 .venv/bin/python scripts/bbywvy_test.py`; its success alone
does not clear the dictionary blocker. Record the dictionary arm's typed
result and artifact hashes in a new receipt before resuming the parent task.

No package, corpus, model, or runtime configuration was changed by this
resolver.
