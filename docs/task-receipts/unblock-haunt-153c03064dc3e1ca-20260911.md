# Unblock receipt: `unblock/haunt/153c03064dc3e1ca/resolve`

- Date: 2026-09-11 UTC
- Owner: `haunt`
- Parent: `haunt-install-unblock-20260907/install-and-retry-tinyfleet`
- Result: **BLOCKED/operator-input**

## Diagnosis

The protobuf/runtime repair and six-case BbyWVY smoke probe are already
verified, but the dictionary arm still has no frozen scientific input. A
repository-wide search found receipts and prose describing the requirement,
but no operator-approved dictionary or lexicon corpus. The missing datum is
not safely inferable from the code or the existing model output.

## Exact operator-action packet

Supply and record all five fields in a tracked frozen study-input manifest:

```text
source: <approved dictionary/lexicon URI or repository-relative path>
language: <ISO language or language set>
normalization: <exact Unicode/token normalization policy>
version: <immutable release, commit, or checksum>
corpus: <immutable corpus path/URI and SHA-256>
```

Do not select a public dictionary or install a package autonomously: that
would invent the experiment's scientific input. No package, corpus, model, or
shared runtime state was changed by this resolver.

## Exact retry condition and command

After the manifest and corpus are supplied with hashes, rerun the parent
dependency preflight and capture versions, command output, artifact hashes,
and the typed verdict. The existing runtime smoke retry is:

```bash
cd /home/mesh-home/tiny-fleet
timeout 240 .venv/bin/python scripts/bbywvy_test.py
```

This receipt alone does not discharge the dictionary blocker or authorize a
study run. Supporting evidence: `docs/task-receipts/haunt-install-unblock-20260908.md`.
